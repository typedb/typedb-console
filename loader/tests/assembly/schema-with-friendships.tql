define
attribute name, value string;
attribute age, value integer;
attribute active, value boolean;
relation friendship, relates friend @card(2);
entity person, owns name, owns age, owns active, plays friendship:friend;
